# Production environment - Frontend only (demo mode)
# Backend tidak di-deploy, frontend akan error saat fetch API
# Untuk demo penuh, deploy backend terpisah atau gunakan mock data
VITE_API_URL=http://localhost:3001/api
